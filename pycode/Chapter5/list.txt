if语句
    每条if语句的核心都是值为tru或false的表达式，被称为条件测试
    true false来决定是否执行后面的代码
注意： = 为赋值  ==为发问（或者说对比）

if语句 and条件需要同时达到为true
       or 条件，只需要达到一个就可以为true
if-elif-else结构，elif可以用多个，if else只能有一个。
if语句可以省略最后的else，用elif代替，但是必须把所有条件都想到，会出BUG

if-elif-else结构只适用于只有一个条件满足的情况，如果有多个条件需要满足
就需要使用多个if结构
